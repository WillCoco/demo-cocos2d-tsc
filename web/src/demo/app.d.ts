declare class HelloWorldLayer extends CCLayer {
    sprite: cc.Sprite;
    constructor();
    ctor(): boolean;
}
declare class HelloWorldScene extends CCScene {
    constructor();
    onEnter(): void;
}
